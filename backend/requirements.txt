    # ── Core framework ────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlmodel==0.0.22
python-multipart>=0.0.9
psycopg[binary]>=3.2.0

# ── Auth (untouched) ──────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
pydantic-settings==2.2.1
email-validator==2.3.0
python-dotenv==1.0.1

# ── HTTP client (used for HF + Groq free API calls) ──────────
httpx>=0.27.0

# ── Audio feature extraction (DSP library, no model download) ─
librosa>=0.10.0
soundfile>=0.12.0

# ── Video frame extraction (library, no model download) ───────
opencv-python-headless==4.11.0.86
opencv-contrib-python==4.11.0.86

# ── Face detection (bundled in mediapipe package, no download) ─
mediapipe>=0.10.0

# ── Fusion Neural Network + training ─────────────────────────
scikit-learn>=1.4.0
numpy==1.26.4

# ── Vector Store & Embeddings (optional) ──────────────────────
chromadb>=0.4.22
sentence-transformers>=2.2.2

# ── LLM & Agent Support ───────────────────────────────────────
openai>=1.3.0
groq>=0.4.1
pydantic>=2.0.0
langgraph>=0.2.40
langchain>=0.2.16
langchain-openai>=0.2.0
langchain-groq>=0.1.9

# ── Geolocation & Maps ────────────────────────────────────────
geopy>=2.4.0
googlemaps>=4.10.0
twilio>=9.0.0

# ── Additional utilities ──────────────────────────────────────
pydantic-settings>=2.0.0
python-dateutil>=2.8.0



dotenv